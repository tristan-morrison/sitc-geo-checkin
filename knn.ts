/**
 * K-Nearest Neighbor functions.
 */

import kdbush from "kdbush";
import geokdbush from "geokdbush";

/**
 * Given a set of points, return the subset that are no more than a given distance away from the given reference point
 * @param thresholdRange  The maximum distance allowed between a point in the result set and the reference point
 */
function filterPointsByThresholdRange(refPt, searchPts, thresholdRange) {
    
}